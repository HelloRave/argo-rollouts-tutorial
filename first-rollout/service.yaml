apiVersion: v1
kind: Service
metadata:
  labels:
    app: simple-color-app
  name: simple-color-app
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: simple-color-app
